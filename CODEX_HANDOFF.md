# Codex handoff

For ordinary work, use `CURRENT_WORKFLOW.md`. This handoff is continuation
context only and never grants authority.
For high-risk work, a conversational approval is sufficient; no copied hash or approval line is required.

## Live operational summary — 2026-08-07

- The active Alpha ladder contract is `d3ab8435...3dfd18` with profile
  `a2088ceb...a53210`. The authoritative calendar/source-observability
  successor is `ddbe0c70...3a7f9`.
- Counted mechanism `cfefe8ce...563dc3` is prepared, unpublished,
  unregistered, and synthetically certified Tier 0 PASS. It excludes only
  authoritative calendar closures, holiday-modified sessions, and explicit
  source-unobservable sessions before fold construction. No return, model,
  prediction, economic result, or 2025 value was opened for it.
- The current and complete high-risk synthetic lanes most recently passed 103
  and 852 tests. A new 100% ES/CL/ZN/6E row-readiness census remains mandatory
  before pilot registration; its plan has not yet been prepared or executed.
- The worktree is being reconciled for an exact local repository checkpoint.
  Nothing is staged or committed. `.pytest_tmp_remaining/` is test residue and
  is ignored; deletion remains separately controlled.
- The chronological sections below preserve earlier states. When they conflict
  with this summary, this summary and `CURRENT_WORKFLOW.md` control ordinary
  work. Immutable registry, receipt, and evidence bytes remain authoritative
  for their individual historical events.

## Freshness and authority

- Reconciled repository: `C:\Users\donny\Desktop\futures_intraday_model_v2`;
  branch `codex/tier1-phase8-economics`; reconcile the live HEAD and Git
  status before relying on this snapshot.
- Historic active-data evidence is intentionally ignored and remains outside
  current work. Its plans and approvals are provenance only.

## Earlier authoritative-trial context (historical)

- Phase 8 economics is complete for the current 41-market/677-interval chain:
  passing Databento audit `efb8943f...638d5`, protected rulebook, and current
  indexed economics receipt `a9656ec5...ff7d`. Continuous-contract rolls are
  boundaries, never price returns.
- CME capture material and its gap reports are historic/reference support only.
  Consult CME only for a Databento audit conflict, a new signature, or an
  unresolved quote/unit interpretation.
- The bracket/authoritative succession history remains immutable provenance.
  It culminated in registered authoritative trial `5b01056d...192fa4`, whose
  one authorized 2018-2022 historical execution durably sealed complete
  unpublished evidence as manifest `c18ef7e9...b4a94`.
- That sealed evidence supports a valid rejection, not an invalid retirement:
  the candidate selected path was complete, inference executed, missing data
  did not help the decision, stress net P&L was `-$1,433.75`, and maximum
  continuous drawdown was `$1,552.50`. The mandatory positive-stress-P&L and
  `$1,500` drawdown gates both failed.
- Valid-rejection closure `42e1f97c...30a80` is published and binds the trial,
  authorization receipt `44a6fc7c...36c6`, sealed manifest, decision,
  inference, runtime receipt, certificate, registry, and former pointer hash.
  No invalid-retirement record references this trial.
- `configs/active_tier1_trial.json` is now a non-executable tombstone with state
  `NO_ACTIVE_TRIAL_VALID_REJECTION`. Active and historical execution authority
  are both false. There is no active Tier 1 trial.
- The 2025 holdout, providers, credentials, trading, and live deployment remain
  untouched by the closure publication.

## Earlier worktree reconciliation snapshot

- The overnight closure and fold-readiness remediation are committed through
  HEAD `efa2b715...34b5`. The worktree was clean at the start of the current
  universal-gateway remediation.
- Current work is limited to the certified gateway, shared operation policy,
  retirement guards and test-lane classification, focused tests, and current
  workflow documents. Nothing is staged or committed from this remediation.
- Preserve all evidence and unrelated work; never broad-stage this worktree.

## Overnight inventory reversal closure and certification remediation

- Materially new deterministic mechanism trial `24772e41...1169c` consumed
  its one authorized 2018-2022 attempt and closed
  `INCONCLUSIVE_DATA_OR_COVERAGE` before outer economic evaluation. The runtime
  found fewer than the locked 252 complete training sessions in the first
  failing fold-market. No return, prediction, or economic decision was
  produced; retry and incremental rescue are forbidden.
- Row-certified report `abc910ff...5b440` now proves the first failure was
  fold 0 ES: 673 expected training sessions, 1 complete, versus the locked 252.
  Exclusions were 664 incomplete-or-identity-changing paths, 5 missing exact
  08:29 bars, and 3 missing overnight endpoints. All 32 fold-market cells fail
  readiness; no economic evaluation occurred.
- Additive unpublished clarification `d4f97ae6...ea643` preserves the original
  closure and classifies the result as source/protocol incompatibility plus a
  preexecution-certification omission, not a strategy loss. The runtime
  enforced the registered identity rule; its operational defect was checking
  too late and discarding exact context.
- Additive remediation now prepares a reusable fail-closed fold certificate,
  45 passing focused tests (60 across the complete applicable
  overnight-remediation suite), and bounded audit-only source census tooling.
  Synthetic results are not real-source certification.
- The governed registration writer now requires an exact canonical evidence
  file and binds its path, SHA-256, certificate ID, trial family, and protocol
  into the registration identity. The execution wrapper reloads and revalidates
  that exact registration and evidence before invoking a single-use claim;
  changed, missing, synthetic, failing, or cross-trial evidence fails first.
- The first approved serial census consumed authorization receipt
  `3b81b12f...e5312` but exceeded its 900-second runtime before producing any
  output. Codex terminated the remaining exact census process; the old plan is
  not retryable and all of its bytes are preserved.
- The separately authorized parallel successor retained the same 20 sources,
  parser, observation builder, fold evidence, and certificate semantics. It
  completed with receipt `6fc820d1...23d0` and produced only the unpublished
  FAIL report; economics, models, predictions, 2025, providers, publication,
  active data mutation, staging, commit, push, and trading remained untouched.
- Closure clarification `d4f97ae6...ea643`, readiness report
  `abc910ff...5b440`, and terminal event `935165e7...622d3` are now published
  create-only. Each published file exactly matches its preparation. Active
  pointer hash `5c7279e3...b7671` and original closure hash
  `d0396acf...55248` remain unchanged.

## Universal certified research gateway

- `CertifiedResearchGateway` is the only current trial-registration and
  historical-execution surface. Registration requires a passing row-certified
  fold certificate; execution reloads the exact registration and evidence
  before consuming a claim.
- The shared receipt boundary admits one fixed certified trial operation and an
  exact allowlist of non-strategy preparatory operations. Unknown and retired
  trial operation names fail before an authorization-use record is written.
- V4-V12, Standard-only, Final, Authoritative, and the former generic
  real-history `TrialRegistry` are preserved as legacy evidence paths. Their
  transition snapshot tests are in the legacy lane; current gateway and valid
  rejection controls remain in the high-risk lane.
- Verification is green: 82 focused gateway/census tests, 103 current-lane
  tests, and 643 complete high-risk-lane tests passed. The one consumed-plan
  pre-execution snapshot assertion is explicitly legacy, not silently skipped.

## Current boundary

- No strategy retry or economic execution is authorized. Publication is
  complete. Universal gateway verification is local and synthetic only; it
  grants no research authority and creates no active trial.
- Staging, commit, push, deletion of retained evidence, holdout access,
  provider access, and trading remain separately controlled operations.

## Prepared Alpha research ladder successor

- Inactive successor contract `d3ab8435...3dfd18` and profile
  `a2088ceb...a53210` define synthetic Tier 0, one 504/63 ES pilot, the exact
  4/16/41 market ladder, mandatory 38/3 Tier 3 reporting, one project-level
  2025 holdout, and forward monitoring only.
- The successor is now published and active. Pointer
  `configs/active_alpha_research_ladder.json` has SHA-256
  `e4274219...5edc9`; its registered-context loader and 81 post-activation
  focused tests passed.
- Unpublished invalid-preparation record `5fab42d1...95c8bd` preserves the
  ES-2018 profile preparation byte-for-byte and classifies the six-market
  protocol as `ALPHA_TIER_SCOPE_MISMATCH`. It also preserves superseded
  preparation `db38f725...e96576`, whose profile incorrectly bound the
  unpublished path instead of its final registry path. None is published.
- The gateway binds the exact ladder contract/profile, frozen mechanism,
  predecessor PASS decision, row-certified stage sessions, and pilot exclusion
  identity. Tier 0 cannot register real history, mechanism drift restarts the
  ladder, Tier 3 traditional failure cannot be rescued by satellites, and a
  project-level 2025 marker prevents repeated holdout claims.
- No historical row, return, model, prediction, provider, credential, 2025
  value, active research data, or trading path was accessed in this work.
- Additive implementation now prepares the ladder-wide mechanism only after
  the ladder is active. It reuses the causal 10:00 reported-bar ridge design,
  but not the invalid six-market scope, and freezes the canonical 41-market
  ranking, provisional stress costs, risk rules, six independent baselines,
  stage promotion gates, and one-attempt progression.
- Pilot registration now also requires an independently validated Tier 1
  readiness certificate and Tier 1 session manifest bound to the same
  mechanism and the exact 63 pilot exclusions. Tier 0 requires a passing
  synthetic certificate; economic progression recomputes the locked gates
  instead of trusting a PASS label.
- Frozen mechanism `186d8a10...5de3f` is sealed unpublished at file SHA-256
  `1b0fa1d2...37798`. Tier 0 certificate `e05d3037...29a05` and predecessor
  PASS decision `6c9aca04...b2175` are also sealed unpublished and validated;
  the preparation suite passed 102 tests.
- The next genuine boundary is one combined read-only 2018-2022 readiness
  census for the exact ES 504/63 pilot fold and the four-market Tier 1 folds.
  It must freeze the pilot sessions, prove Tier 1 after the 63-session
  exclusion, and emit no returns, models, predictions, or economic results.

## Prepared cash-open impulse mechanism

- One new Tier 1 mechanism is prepared but not registered: market-specific
  cash-open impulse continuation at causal 09:00 and 10:30 Chicago checkpoints.
  Its exact prepared protocol ID is
  `3b8e09d65015afd33fc033aa72c8bb0be22425cafac8b8b145eeccb639258067`.
- Each opportunity requires 30 exact feature bars, a decision five seconds
  after the last bar becomes available, entry one full minute later, and a
  31-bar same-contract execution path. Rankings compare only the four markets
  observable at the same checkpoint.
- A prepare-only single-use census is implemented. It must prove every one of
  the 32 fold-market cells, every required baseline, every feature/path, every
  market-year, and scenario-specific risk terminalization against the 20 bound
  immutable sources before registration is possible.
- Synthetic mechanics are green but real-source sufficiency is deliberately
  unknown. The first approved census consumed use record `9f2eb538...f3e4`
  but Windows denied multiprocessing pipe creation before workers started;
  zero rows were decoded and no report was created. The plan is not retryable.
- A hash-bound V2 successor preserves the failed plan/use and changes no
  research semantics. Its only operational change is explicit Windows host
  permission for the same four-worker process pool. The next genuine boundary
  is a new approval for that $0 read-only 2018-2022 census. No model fit,
  prediction, return, performance evaluation,
  publication, registration, 2025, provider, active-data, or trading action is
  part of that census.
- The approved V2 host successor completed under receipt `76915dcc...36fa`
  and sealed unpublished report `5c7fe871...6812` (file SHA-256
  `57b6d368...8570`). Its row-certified decision is `FAIL`; registration and
  execution authority are false.
- This is a pre-registration source-compatibility rejection, not an economic
  outcome. Eleven of 32 fold-market cells passed and 21 failed. The full census
  found 79 incomplete/identity-changing execution paths and 66 incomplete/
  identity-changing feature windows. ZN failed all eight cells. Fold 2 had only
  62 calendar-eligible sessions against the locked 63-session minimum.
- No model, prediction, return, P&L, or performance statistic was computed.
  Do not register or incrementally tune this mechanism. Any future candidate
  must be materially new or use a newly justified immutable source family and
  must repeat pre-registration certification.
- The completed dependency-forensics report remains sealed at file SHA-256
  `c8e4b058...53ccb`. Its 35 apparent `ENTRY_NOT_AFTER_DECISION` findings were
  downstream consequences of unavailable feature inputs, not genuine early
  entries; the additive correction retains zero genuine entry-order failures
  and does not alter the sealed report.
- Corrected fold construction now uses the intersection of explicitly
  checkpoint-eligible calendar sessions. The authoritative four-market
  calendar supplies 1,291 common eligible sessions and supports eight exact
  63-session tests with one embargo session; generic date windows are no
  longer accepted.
- Source selection is centralized through the active catalog resolver. The
  41-market 2018-2022 matrix has 205 expected market-years: 198 resolvable,
  four quarantined, and three absent. Direct paths, globs, newest-release
  selection, and fallback releases are forbidden.
- Prepared census plan `dc2910b0...62d2` is deliberately blocked and cannot
  read rows. The authoritative historical checkpoint calendar covers only 6E,
  CL, ES, and ZN; the other 37 markets require a certified immutable calendar
  expansion before a 41-market compatibility census can be authorized.
- The approved local-reference-only calendar source-adequacy audit parsed 64
  immutable CME XLS workbooks/ZIP members and sealed unpublished report
  `d9e7d196...5b0fc` (file SHA-256 `ef62fa55...503a`). Broad CME family labels
  are present in every 2018-2022 year, so the source folder is not the problem.
- The audit still fails closed for an exact 41-market successor: 137
  market-years lack a certified open/halt translation over the mechanism's
  exact 08:29-11:01 dependency horizon; 45 agricultural market-years lack a
  bound grain/oilseed-versus-livestock row mapping; and ETH has no bound
  2018-2020 asset-class reference. All 135,124 affected market/date/checkpoint
  states remain explicitly unverified. No price row or economic result was
  read or computed.
- The approved reference remediation now binds all 41 markets to exact CME
  schedule families, all 205 market-years to hash-verified definition files,
  and the economics rulebook's exact product authorities. Prepared inactive
  successor `a6365e1f...29584` contains all 74,866 market-date rows plus exact
  merged product-effective intervals and remains unpublished with file SHA-256
  `69b3cef6...097e`.
- The successor correctly remains `FAIL_UNRESOLVED_REFERENCE_COVERAGE`: the
  bound archive labeled `2019_annual` contains the 2019-2020 New Year schedule,
  not the January 1, 2019 schedule. Forty then-effective markets therefore
  have 80 unverified checkpoint states on that one date; ETH was not yet an
  effective product. Those states are explicit abstentions, not inferred
  closures. No price rows, 2025 data, provider, model, return, publication,
  activation, or active pointer were touched.
- A separately approved local immutable alternative search sealed record
  `37c90ab2...2adcb` (file SHA-256 `515cc166...43c0`). It inventoried 19
  calendar/reference releases and searched the five exact-scope CME captures,
  including 64 workbook members, structured notice pages, 2018-2019 notice
  documents, and locally captured 2018-2019 attachments. No artifact provides
  complete January 1, 2019 product-family schedule evidence. Decision:
  `FAIL_NO_EXACT_LOCAL_SOURCE`; the 80 abstentions remain mandatory.
- A bounded official-CME/Internet-Archive recovery then found snapshot
  `20180508163804` of CME's exact 2018-2019 New Year compact Globex workbook.
  Raw SHA-256 is `2684a5f1...c188`; acquisition record `fbca0af0...55161`
  (file SHA-256 `2c0dcb6c...f0dc`) validates explicit January 1 closure for
  equity, Bitcoin, rates, FX, energy/metals, grain/oilseed, and livestock.
  The bytes remain immutable inactive staging and are not published or
  activated.
- A local reference-only integration preserved the failed `a6365e1f...29584`
  preparation and produced inactive successor `54bc5550...0397` (file SHA-256
  `7860a57f...95ec`). It is `PASS_EXACT_REFERENCE_COVERAGE`, has all 74,866
  market-date rows, and has zero unresolved reference states. Relative to the
  failed preparation, exactly 40 January 1, 2019 market-date rows changed:
  exactly 80 checkpoint dispositions moved from explicit unverified
  abstention to verified CME-family closure. ETH remains excluded because it
  was not yet effective; every other row is byte-equivalent at the data-model
  level.
- Successor `54bc5550...0397` is now published as an exact-byte immutable copy
  and activated through the dedicated mechanism-specific pointer
  `configs/active_cash_open_impulse_historical_calendar.json`. Registration
  `c8704c80...7eb0`, event `724a794a...3a1`, and pointer `1cece821...e8d1`
  bind the complete closure. The general forward exchange-calendar pointer and
  the prior Tier 1 historical-calendar pointer remain byte-for-byte unchanged;
  their schemas and authority scopes are deliberately separate. The first
  post-activation test launch could not create its Windows root temporary
  directory, so the dedicated pointer was removed immediately, the focused
  suite was rerun through the approved host launcher, and the exact immutable
  publication was reactivated. Final validation passed 20 focused tests and
  684 complete high-risk tests (767 deselected). No price rows, 2025 data,
  provider, credential, model, trial, or trading path was accessed.
- The eight-finding cash-open remediation is implemented through the next
  high-risk boundary. The rejected four-market protocol is permanently denied
  by the certified gateway and has unpublished closure `136f0856...6628e`.
  Current source access is active-catalog-only; the four prior cash-open
  forensic/readiness operation names are retired before claim consumption.
  Corrected inactive grid calendar `cd64f912...0843b` adds `09:30` and `10:00`
  while preserving every existing `09:00` and `10:30` state; it has 74,866
  rows and zero unresolved states. The earlier `fa9dc5cd...46105` preparation
  is preserved invalid because it bound the mutable active-pointer path.
  Source-only specification `56d6f163...be41` freezes the 41-market, 100%-path,
  coverage-only selection and fallback rules. No final census plan can be
  created until the corrected calendar is separately published and activated;
  no historical price row or economic outcome was opened in this remediation.
