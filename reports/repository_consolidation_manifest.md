# Repository consolidation manifest

## Status

Prepared locally. Nothing is staged or committed.

This manifest converts the accumulated Alpha-ladder, calendar, readiness, and
pipeline-documentation work into one exact fail-closed local checkpoint scope.
It authorizes no staging, commit, push, deletion, historical-row access,
publication, provider access, holdout access, or trading.

## Live binding

- Repository: `C:/Users/donny/Desktop/futures_intraday_model_v2`
- Branch: `codex/tier1-phase8-economics`
- HEAD: `8bc3f1b3f3aea7d4742e2f33032307d83519571d`
- Exact pre-manifest path inventory:
  `reports/repository_consolidation_path_inventory.json`
- Inventory SHA-256:
  `e8b1a8a5d14c44ea5b3509d74fb976ee8f2d48c6ff9cd26f4db8cf139c9159bd`
- Inventory content snapshot SHA-256:
  `c07eb685e7ca91af940cc3b8ebc5778563e6a7694c4efc2573615c96f7803e94`
- Inventory paths: 313
- Exact eventual staging scope: those 313 paths, the inventory itself, and
  this manifest; total 315 paths.

Staging must fail closed if the repository root, branch, HEAD, inventory hash,
selected path set, selected file bytes, staged path set, or presence of any
additional Git-visible path differs.

## Classification

Every one of the 313 inventoried paths was reconciled:

- 74 current Alpha/calendar/gateway implementation, tests, active bindings,
  operational documentation, and the counted `cfefe8ce...563dc3` Tier 0
  mechanism.
- 237 deliberately preserved historical plans, scripts, tests, receipts,
  reports, failed attempts, calendars, mechanisms, closures, and source-only
  evidence. They remain evidence and do not regain operating authority.
- 2 separately scoped static pipeline-map documents. They remain a dated
  inspection snapshot and do not grant protected-data or execution authority.
- 0 suspicious or unrelated paths.
- 0 credential-bearing paths.
- 0 raw price, feature, outcome, prediction, evaluation, or holdout payloads.

The eight historical scripts that could otherwise look operational are now
named explicitly in `docs/LEGACY_WORKFLOWS.md`. They are not packaged entry
points and are not current instructions.

## Large immutable evidence

The scope deliberately preserves 11 calendar JSON copies totaling 206,426,245
working-tree bytes. They represent eight unique immutable blobs; three pairs
are byte-identical publication/preparation copies. Five unique superseded
calendar preparations total 84,418,451 bytes. They are retained intentionally
because later records bind their exact identities. No large file is being
silently discarded merely to make Git status shorter.

## Excluded runtime residue

`.pytest_tmp_remaining/` contains abandoned synthetic-test sandboxes. It is now
ignored by Git and is not part of the 315-path staging scope. Its bytes remain
on disk because deletion requires separate approval. No research artifact is
stored there.

## Required validation before staging

1. Parse every selected JSON and YAML file.
2. Compile the selected Python source, scripts, and tests.
3. Run the complete current and high-risk synthetic lanes.
4. Revalidate every active pointer and the counted Tier 0 certificate.
5. Run the credential-pattern scan and `git diff --check`.
6. Recompute this inventory and require an exact match.

## Commit boundary

After exact staging is separately approved and verified, a local commit still
requires its own explicit approval. No push is included.
