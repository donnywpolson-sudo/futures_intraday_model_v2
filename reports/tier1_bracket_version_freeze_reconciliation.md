# Tier 1 successor version freeze and reconciliation

Status: governance-only preparation. This report does not register, retire,
publish, execute, or activate a trial.

## Authoritative current answer

There is no execution-ready Tier 1 successor in this checkout.

- V2 is invalid because of causal timing, fill, and coverage defects.
- V3 through V10 have published retirement records.
- V11 executed, is invalid for a flat-baseline feature-coverage dependency,
  and has a prepared but unpublished retirement.
- V12 is a prepared contract, not a registered trial. Its source sufficiency
  is not durably proven.
- No V13 or V14 artifact, ref, tag, worktree, or local-history entry exists.

The phrase "on V14" therefore has no repository-backed meaning here. Version
creation is frozen. The next candidate remains unversioned until one complete
pre-execution certificate passes.

## Why this is progress but not strategy evidence

The sequence produced real improvements in causality, coverage, marked risk,
baseline independence, cost handling, statistical alignment, bounded source
streaming, evidence identity, and authorization. It did not produce a valid
promotion or rejection. V9 and V11 reached evaluation but were invalidated by
implementation defects; the other successors stopped earlier.

## Required next state

One certificate must prove the complete synthetic pipeline, every adversarial
control, immutable source sufficiency, protocol-to-code agreement, one source
set, one current-trial pointer, and continued 2025 isolation. A versioned trial
may be created only after that certificate passes.

The machine-readable disposition ledger is
`configs/tier1_bracket_version_freeze_reconciliation.json`.
